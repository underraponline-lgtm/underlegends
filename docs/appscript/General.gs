// ═══ GENERAL.GS — OBSOLETO (migrado a GitHub Actions) ═══
//
// Las funciones que vivían aquí (syncMembers, onEdit, fixFechas,
// debugCheck, postToDiscord) operaban sobre la hoja "Control" del
// Oficial. Toda esa lógica fue reemplazada por el bot de GitHub Actions
// (repo: underraponline-lgtm/liga-global-sync, sync.py), que ahora:
//   - Sincroniza verificación en la Lista de Raperos del Operativo (col D)
//   - Actualiza avatares en la Lista del Operativo (col F)
//
// La hoja "Control" fue eliminada. No agregar funciones que la usen.
// El perfil web app vive en WebApp.gs + Index.html (NO tocar acá).
//
// Última limpieza: 30 mayo 2026